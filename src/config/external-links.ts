/**
 * External profile links for the public landing page footer.
 * Set URLs when ready; links are hidden until configured.
 */
export const externalLinks = {
  linkedin: null as string | null,
  github: null as string | null,
} as const;
